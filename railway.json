{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "rootDirectory": "agent-engine",
        "watchPatterns": [
            "agent-engine/**"
        ]
    },
    "deploy": {
        "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
}